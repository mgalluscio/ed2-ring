# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ed2-ring' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ed2-ring
  pod 'AWSCore', '~> 2.11.0'
  pod 'AWSAppSync', '~> 2.14.2'
  pod 'AWSMobileClient', '~> 2.11.0'      # Required dependency
  pod 'AWSAuthUI', '~> 2.11.0'            # Optional dependency required to use drop-in UI
  pod 'AWSUserPoolsSignIn', '~> 2.11.0'   # Optional dependency required to u
  pod 'AWSS3', '~> 2.11.0'   # For file transfers

end
